version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app

  frontend:
    build: ./ProyIHC-MiniApp
    ports:
      - "80:80"
    volumes:
      - ./ProyIHC-MiniApp:/app
